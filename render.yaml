services:
  - type: web
    name: cascade-backend
    env: python
    buildCommand: "cd backend && pip install -r requirements-serve.txt"
    startCommand: "cd backend && uvicorn serve.api:app --host 0.0.0.0 --port $PORT"
    envVars:
      - key: PYTHON_VERSION
        value: 3.10.0
